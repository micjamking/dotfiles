# ---------------------------------------------- #
# Exports / Sources
# ---------------------------------------------- #

# Paths
export ANDROID_HOME=~/Library/Android/sdk/
export PATH=${PATH}:~/Library/Android/sdk/platform-tools:~/Library/Android/sdk/tools:~/Library/Android/sdk/build-tools/23.0.3
export JAVA_HOME=$(/usr/libexec/java_home)
export PATH=${JAVA_HOME}/bin:$PATH
export PATH=/usr/local/bin:/usr/local/sbin:$PATH
export PATH=$HOME/.scripts/node:$HOME/.scripts/bash:$PATH
#export PATH=$(brew --prefix coreutils)/libexec/gnubin:$PATH
#export GIT=/usr/bin/git
export GS_ACCESS_KEY_ID="GOOGYQXURUP5VZTFDEZN"
export GS_SECRET_ACCESS_KEY="CGfaUDW45Jox+H6o7eV9a0yWJJU0Lym4yD0tJ6bF"

# Language Support
export LC_CTYPE=en_US.UTF-8
export LC_ALL=en_US.UTF-8

# Bash auto-completion
if [ -f $(brew --prefix)/etc/bash_completion ]; then
  source $(brew --prefix)/etc/bash_completion
fi

# Google Cloud SDK
source '/Users/kingmi/google-cloud-sdk/completion.bash.inc'
source '/Users/kingmi/google-cloud-sdk/path.bash.inc'

# Node / NVM
export NVM_DIR=$HOME/.nvm
[ -s "$(brew --prefix nvm)/nvm.sh" ] && source $(brew --prefix nvm)/nvm.sh

# Ruby / RVM
export PATH="$PATH:$HOME/.rvm/bin"
[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm"

# Travis
[ -f /Users/Zion/.travis/travis.sh ] && source /Users/Zion/.travis/travis.sh

# ENV
export FILENAME="$(basename $(pwd))"
